#include "pqueue.h"

int main (void){
    int a[]={0, 10, 2, 4, 1, 23};
    pqueue_sort(a, 1, 5);
    // Pqueue p=malloc(sizeof(struct pqueue));
    // int n=10; //creo una pqueue con uno heap grande 10
    // p=pqueue_new(n);
    // pqueue_insert(p, 10);
    // pqueue_insert(p, 2);
    // pqueue_insert(p, 3);
    // printf("lala \n");
    // stampa(p);
    //printf("lala %d\n", *(p->h+p->count));

}